<div>
  <h4>Comment by: <%= comment.user_name %></h4>
  <p class = "comment"><%= comment.body %></p>
  <p>Posted <%= distance_of_time_in_words(comment.product.created_at, comment.created_at) %> later</p>
  </div>
